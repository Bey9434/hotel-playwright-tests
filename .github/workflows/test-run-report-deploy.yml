name: Test and Deploy Playwright Report

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0"

      # プロジェクトディレクトリに移動してPlaywrightパッケージを追加
      - name: Add Playwright package
        run: |
          cd HotelPlaywrightTests  # プロジェクトのパスに移動
          dotnet add package Microsoft.Playwright

      # プロジェクトをビルド
      - name: Build the project
        run: dotnet build

      # Playwrightブラウザのインストール
      - name: Install Playwright browsers
        run: pwsh bin/Debug/net8.0/playwright.ps1 install

      - name: Run Playwright tests
        run: dotnet test --logger:html --results-directory docs/

      - name: Rename report to index.html
        run: mv HotelPlaywrightTests/docs/TestResults/*.html HotelPlaywrightTests/docs/index.html

      - name: Commit and push changes
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add HotelPlaywrightTests/docs/
          git commit -m "Add Playwright report"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
